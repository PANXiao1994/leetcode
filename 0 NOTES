19 Length of Last word
先把最后一个词后面的空格都忽略掉，再计数。

35 Majority Element II
i)如果除去maj1(maj2)之后，maj2(maj1)占数超过一半，那么maj2在总的array里一定超过1/3
第一部分的代码得到的maj2一定是去掉maj1(maj2)之后出现频率最高的数
ii)如果...之后，...占数不到一半,那么第一部分的代码得到的数并不一定是...

34 Majority Element
同理，如果不满足先决条件（一定有元素占一半以上）,那么该算法也得不到出现频率最高的数。
也就是说该算法得到出现频率最高的数，是以该数占一半以上为前提的。

46 Convert a Number to Hexadecimal
☆《看一下数据类型和范围》

22 Add Binary 和 48 Add Strings几乎一样
